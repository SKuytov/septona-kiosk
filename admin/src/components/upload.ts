import { apiUrl, authStorage } from '../api'
import type { ApiError } from '../types'

export function isPdf(file: File) { return file.type === 'application/pdf' || file.name.toLowerCase().endsWith('.pdf') }
export function validatePdf(file: File) { if (!isPdf(file)) return 'Файлът не е PDF и не може да бъде качен.'; if (file.size > 100 * 1024 * 1024) return 'Файлът надвишава максималния размер от 100 MB.'; return null }
export function uploadPdf<T>(path: string, file: File, extra: { meta?: unknown; note?: string } = {}, onProgress?: (value:number)=>void) { return new Promise<T>((resolve,reject)=>{ const form=new FormData(); form.append('file',file); if(extra.meta!==undefined) form.append('meta',JSON.stringify(extra.meta)); if(extra.note!==undefined) form.append('note',extra.note); const xhr=new XMLHttpRequest(); xhr.open('POST',apiUrl(path)); const token=authStorage.get(); if(token)xhr.setRequestHeader('Authorization',`Bearer ${token}`); xhr.upload.onprogress=e=>{if(e.lengthComputable)onProgress?.(Math.round(e.loaded/e.total*100))}; xhr.onerror=()=>reject({status:0,message:'Няма връзка със сървъра. Опитайте отново по-късно.'} satisfies ApiError); xhr.onload=()=>{const body=xhr.responseText?JSON.parse(xhr.responseText):undefined;if(xhr.status>=200&&xhr.status<300)resolve(body as T);else {if(xhr.status===401){authStorage.clear();window.dispatchEvent(new Event('septona:unauthorized'))}reject({status:xhr.status,code:body?.error?.code,message:body?.error?.message||'Качването не бе успешно.',payload:body} satisfies ApiError)}}; xhr.send(form) }) }
